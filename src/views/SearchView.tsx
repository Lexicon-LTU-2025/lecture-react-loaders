import { SearchForm, SearchResults } from '../components';

export const SearchView = () => {
  return (
    <main id="search-view">
      <h2>Search View</h2>
      <SearchForm />
      <SearchResults />
    </main>
  );
};
